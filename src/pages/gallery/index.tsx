
// Remove "use client" directive if present and keep the metadata export
import type { Metadata } from 'next';


export default function GalleryPage() {
    return (
        <section>
            <div>GalleryPage</div>
        </section>
    );
}